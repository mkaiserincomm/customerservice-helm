apiVersion: logging.banzaicloud.io/v1beta1
kind: Flow
metadata:  
  name: {{ include "customerservice.fullname" . }}
  namespace: {{ .Release.Namespace }}
spec:
  globalOutputRefs: []
  localOutputRefs:
  - {{ include "customerservice.fullname" . }}
  match:
  - select:
      container_names: []
      hosts: []
      labels:
        app.kubernetes.io/name: {{ include "customerservice.fullname" . }}